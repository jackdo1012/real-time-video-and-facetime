
class AuthControllers {
    // [POST]/login
    login = (req, res) => {
        try {
            const accessToken = req.body.accessToken
        } catch (error) {
            
        }
    }
}

module.exports = new AuthControllers()